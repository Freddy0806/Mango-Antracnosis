flet
opencv-python
numpy
pillow
# pandas (Solo para scripts batch offline)
# tensorflow (Eliminado para móvil)
tflite-runtime; platform_system == 'Linux' and platform_machine != 'x86_64' # Solo para el compilado Android (ARM)
# tensorflow-cpu; platform_system != 'Linux' # Opcional para dev local
# scikit-learn (Solo para entrenamiento)
# matplotlib (Solo para entrenamiento)
